import { Box } from "@mui/material";
import React from "react";

const Setting = () => {
  return (
    <Box
      style={{
        padding: "15px",
      }}
    >
      Setting
    </Box>
  );
};

export default Setting;
